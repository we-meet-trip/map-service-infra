# 클라우드 배포 — 운영은 네이버 클라우드, 시험은 GCP

늘 켜 두는 서버를 세우는 절차다. 맥에서 터널을 띄우던 방식은 주소가 열 때마다
달라지고 맥이 잠들면 함께 끊기므로 여기서는 쓰지 않는다.

두 환경은 **서로 다른 클라우드에 둔다.** 한쪽을 잘못 건드려도 다른 쪽이
멀쩡하고, 발급처 하루 한도를 나눠 쓰는 사고도 계정 자체가 달라 생기지 않는다.

## 1. 무엇을 어디에 두는가

| | 운영 | 시험 |
|---|---|---|
| 클라우드 | 네이버 클라우드 (VPC) | GCP |
| 구성 | `full` (저장소 2 + 앱 3 + 관문) + 앞단 | 같음. 경로 엔진·카메라·모니터링 제외 |
| 환경파일 | `.env` | `.env.test` (`make-test-env.sh` 로 서버에서 만든다) |
| 외부 호출 | 실제 발급처 | 전부 스텁 (`PLACES_STUB_MODE=true`) |

시험에서 외부를 끄는 이유는 비용이 아니라 **운영이 함께 멈추기 때문**이다.
기상청 키와 두루누비 키가 같은 발급 계정을 쓰므로, 시험이 하루 한도를
소진하면 운영의 예보 폴링과 코스 동기화가 같이 죽는다.

## 2. 서버 크기 — 실측에서 뽑은 값

같은 스택을 도커에 띄워 잰 유휴 사용량이다.

| 컨테이너 | 유휴 | 건 상한 |
|---|---|---|
| user (BFF) | 667 MiB | 2g |
| postgres | 217 MiB | 1g |
| hub | 114 MiB | 512m |
| agent | 104 MiB | 512m |
| redis | 10 MiB | 256m |
| 관문 | 9 MiB | 128m |
| **합계** | **약 1.1 GiB** | 약 4.4 GiB |

- **운영: 4 vCPU / 8 GB / 디스크 60 GB.** 상한 합이 4.4 GiB 이고 여기에 운영체제와
  도커, 그리고 이미지를 서버에서 직접 빌드하는 몫이 더 든다.
  **경로 엔진을 함께 올려도 메모리를 더 잡을 필요는 없다.** 데이터를 통째로
  올려 두지 않고 파일에 그대로 붙여 읽기 때문이다. 재 보니 유휴 약 150 MiB
  둘 합쳐서이고, 요청이 몰리면 상한(각 1 GiB)까지 캐시가 차다가 거기서 묶인다.
  드는 것은 **디스크 약 6.5 GB**(도보·자전거 각 3.1 GB + 원본 272 MB)다.
  카메라를 올릴 계획이면 이미지가 약 3.2 GB 더 든다.
- **시험: 2 vCPU / 4 GB / 디스크 50 GB.** 시험 오버레이가 상한을 절반 이하로
  낮춰 합이 약 2.4 GiB 다.
- **x86-64 를 쓴다.** 저장소 이미지와 경로 엔진 이미지가 amd64 전용이라
  ARM 서버에서는 흉내 내기로 돌아 느려진다.

### 무료 등급 서버(1 GB)에 얹을 때

`docker-compose.micro.yml` 을 시험 오버레이 뒤에 덧칠하면 들어가기는 한다.
실제로 얹어 재고 기능 검사까지 돌린 값이다.

| | 값 |
|---|---|
| 유휴 합계 | 약 700 MiB |
| 기능 검사를 돌리는 중 | **717 MiB** (BFF 가 상한의 91%) |
| 서버 총 메모리 | 1024 MiB |
| 운영체제 + 도커 | 약 200~250 MiB |
| 남는 여유 | **약 60~100 MiB** |

들어가지만 여유가 없다. 이 크기에서 조심해야 할 것이 셋이다.

1. **스왑을 2 GB 붙인다.** 붙이지 않으면 잠깐의 몰림에도 무언가가 죽는다.
2. **이 서버에서는 이미지를 만들 수 없다.** BFF 빌드만 1 GB 를 넘게 쓴다.
   `docker-compose.registry.yml` 로 받아 쓴다(5.3 참고).
3. **부하를 재는 데 쓰면 안 된다.** 좁혀 둔 상한이 먼저 병목으로 잡혀,
   실제 한계가 아니라 이 설정의 한계를 재게 된다.

그리고 이 크기에서는 고장이 눈에 잘 띄지 않는다. 상한을 더 조여 봤더니
BFF 가 기동 도중 메모리 부족으로 죽었는데, **컨테이너는 살아 있고 자원 한도에
걸린 표시도 남지 않아 밖에서는 정상으로 보였다.** 요청을 보내야 502 로
드러났다. 그래서 이 서버에 올린 뒤에는 상태 확인만이 아니라 실제 요청을
한 번 보내 봐야 한다.

## 3. 바깥으로 여는 자리

애플리케이션 포트는 전부 루프백에만 묶여 있다. 그대로 둔다. 바깥으로 여는
것은 앞단의 80·443 둘뿐이고, 관리자 접속용 22 는 본인 주소에서만 연다.

| 방향 | 포트 | 대상 |
|---|---|---|
| 들어옴 | 22 | 본인 주소만 |
| 들어옴 | 80, 443 | 전체 (인증서 발급과 서비스) |
| 나감 | 443 | 발급처·모델·인증서 |
| 나감 | 80 | 서울 열린데이터광장 하나. 그쪽이 https 를 받지 않는다 |

**관문을 직접 열지 않는다.** 관문은 요청자 주소를 `CF-Connecting-IP` 헤더에서
읽고, 사설 대역에서 온 그 헤더를 믿는다. 관문이 바깥에 직접 노출되면 누구나
그 헤더를 적어 보내 분당 상한을 피할 수 있다. 실제로 확인했다 — 관문에 직접
보내면 적어 보낸 주소가 그대로 기록되고, 앞단을 거치면 지워진다.

앞단(`docker-compose.edge.yml`)은 그 헤더를 **먼저 지우고 자기가 본 실제
주소로 다시 채운다.** 그래서 관문 설정을 한 줄도 바꾸지 않고 상한이 계속
사용자별로 걸린다.

## 4. 주소 — 도메인이 없을 때

앱은 https 주소만 받는다(평문은 배포본에서 닫혀 있고 iOS 는 아예 열지 않는다).
도메인을 사지 않고 고정 https 주소를 얻는 길은 셋이다.

| 방법 | 주소 모양 | 대가 |
|---|---|---|
| 무료 동적 DNS (DuckDNS 등) + 앞단이 인증서 발급 | `이름.duckdns.org` | 무료. 제공자가 멈추면 함께 멈춘다 |
| IP 를 이름으로 바꿔 주는 서비스(sslip.io 등) | `1-2-3-4.sslip.io` | 무료. 서버 주소가 바뀌면 이름도 바뀐다 |
| Cloudflare 터널 + 도메인 | `api.내도메인` | 도메인 값만 든다. 들어오는 포트를 아예 안 열어도 된다 |

무료 동적 DNS 를 쓰기로 했다. 이름은 둘이다.

| | 이름 | 주소 |
|---|---|---|
| 운영 | `mapapp` | `https://mapapp.duckdns.org` |
| 시험 | `mapapptest` | `https://mapapptest.duckdns.org` |

운영 서버의 `.env`:

```
EDGE_DOMAIN=mapapp.duckdns.org
EDGE_EMAIL=mapadmin26@gmail.com
DUCKDNS_SUBDOMAIN=mapapp
DUCKDNS_TOKEN=<duckdns 계정 토큰>
```

시험 서버의 `.env.test`:

```
EDGE_DOMAIN=mapapptest.duckdns.org
EDGE_EMAIL=mapadmin26@gmail.com
DUCKDNS_SUBDOMAIN=mapapptest
DUCKDNS_TOKEN=<위와 같은 값>
```

토큰은 이름마다가 아니라 **계정마다 하나**라 두 서버가 같은 값을 쓴다.
그래서 한쪽 서버가 상대의 이름도 바꿀 수 있다 — 시험 서버를 남에게 맡기게
되면 계정을 나누는 편이 낫다.

`--profile dns` 로 함께 띄우면 5분마다 지금 주소를 알려 이름이 따라온다.
클라우드 서버의 바깥 주소는 껐다 켜면 바뀌는데, 바뀐 사실은 앱을 열어 봐야
드러나기 때문이다. **발급처는 실패를 200 응답에 `KO` 라는 본문으로 알린다** —
응답 코드만 보면 성공으로 읽히므로 갱신 컨테이너가 본문을 확인하고 실패를
로그에 남긴다.

주소가 정해지면 앱을 다시 빌드할 필요가 없다 — 게시된 설정 파일의 주소만
바꾸면 앱이 다음 실행에서 새 주소를 쓴다.

## 5. 순서

### 5.1 서버 준비 (운영·시험 공통)

1. 우분투 22.04 서버를 만든다. 방화벽은 3장 표대로.
2. 도커 엔진과 compose 꾸러미를 **도커 공식 저장소에서** 넣는다.
   배포판이 주는 꾸러미는 이 저장소가 쓰는 오버레이 문법을 모른다.
3. 여섯 레포를 나란히 받는다(`map-service-*` 가 같은 부모 아래 있어야 한다).

### 5.2 비밀값 넣기

`.env` 를 **ssh 로 복사**한다(`scp`). 권한은 600.

- 클라우드 콘솔 화면에 붙여 넣지 않는다 — 브라우저 이력과 콘솔 기록에 남는다.
- ssh 로 `echo ... >> .env` 하지 않는다 — 셸 이력에 남는다.
- 시험 서버의 `.env.test` 는 복사하지 말고 **그 서버에서 만든다**:
  `./scripts/make-test-env.sh`. 본보기 파일에서만 파생시키므로 실제 키가
  넘어갈 길이 구조적으로 없다.

옮긴 뒤 지금 `.env` 에 있는 값들은 한 번 갈아 준다. 그 값들은 노트북과
임시 터널을 거쳤다.

운영 `.env` 에서 **반드시 바꿀 것**:

| 키 | 지금 | 운영 |
|---|---|---|
| `TESTER_SEED_ENABLED` | `true` | `false` |
| `AUTH_ENFORCED` | `false` | `true` |
| `ADMIN_SESSION_COOKIE_SECURE` | 없음 | `true` |
| `CORS_ALLOWED_ORIGINS` | 호스팅 주소 | `https://mapapp.duckdns.org,https://mapcenter-b59ca.web.app` |
| `CHAT_INVITE_BASE_URL` | 없음(기본값 사용) | `https://mapcenter-b59ca.web.app/invite/` |
| `LOCATION_ENC_KEYS` | 개발용 값 | `openssl rand -base64 32` 로 새로 |

`TESTER_SEED_ENABLED=true` 는 비밀번호가 공유된 계정 다섯을 부팅마다 만든다.
주소를 모르는 임시 터널 뒤에서는 가려져 있었지만 고정 주소에서는 그렇지 않다.

`AUTH_ENFORCED=true` 로 켜면 서명 열쇠(`JWT_PRIVATE_KEY`/`JWT_PUBLIC_KEY`)가
있어야 부팅한다. 없으면 뜨다 멈춘다.

**초대 주소는 서버 주소와 다르다.** 초대 링크가 앱을 열게 하는 검증 파일
(`assetlinks.json`)이 호스팅 쪽에 올라가 있어서, 그 파일이 있는 주소를 그대로
써야 한다. 서버 주소로 바꾸면 링크가 브라우저로만 열린다.

### 5.3 기동

```
./scripts/cloud-up.sh                        # 운영
./scripts/cloud-up.sh --test                 # 시험
./scripts/cloud-up.sh --edge                 # 바깥 노출까지 함께
./scripts/cloud-up.sh --routing --edge       # 경로 엔진까지 함께
```

**`--micro` 는 1GB 급 서버에서만 붙인다.** 그보다 큰 서버에 붙이면 얻는 것 없이
BFF 가 상한에 붙어 돈다 — 재 보니 정점이 상한과 같고 천장에 서른 번 닿았다.
죽지는 않지만 여유가 없고, 그 사실은 어느 로그에도 남지 않는다.

```
```

**경로 엔진은 운영에만 올린다.** 시험은 발급처를 전부 스텁으로 두므로 경로도
같은 자리에서 스텁으로 답한다 — 시험 서버에 6.5 GB 를 더 둘 이유가 없다.

경로 엔진을 처음 켜기 전에 데이터를 한 번 만들어야 한다. 내려받기와 손질에
시간이 걸리고, 만드는 동안 디스크를 원본만큼 더 쓴다.

```
./scripts/osrm-rebuild.sh
```

`cloud-up.sh --routing` 은 데이터가 없으면 그 자리에서 멈추고 무엇을 해야
하는지 알려 준다. 데이터 없이 엔진만 켜면 뜨자마자 죽는데, 그 모습은 다른
기동 실패와 구분되지 않기 때문이다.

**주소를 채워 두고 엔진을 안 켜는 상태가 가장 나쁘다.** hub 는 주소가 비어
있을 때만 스텁으로 답한다. 채워져 있으면 진짜로 부르러 갔다가 실패하고,
그 실패는 담아 두지도 않아 일정을 열 때마다 구간 수만큼 되풀이된다. 화면에는
도로를 따르지 않는 직선이 그려진다. 엔진을 안 올릴 거라면 `.env` 의
`OSRM_FOOT_BASE_URL` 과 `OSRM_BICYCLE_BASE_URL` 을 **비워야** 한다.

이미지는 **레포의 배포 실행이 만들어 올린다.** 사람이 만들지 않는다.

GitHub 의 `map-service-infra` → Actions → `image-release` → Run workflow.
받아올 지점(기본 `verify/integration`)과 판 이름을 넣으면, 네 레포를 그 지점에서
받아 세 이미지를 **한 이름으로** 만들어 `ghcr.io/we-meet-trip` 에 올린다.
끝나면 실행 화면에 세 커밋과 서버에 옮겨 적을 두 줄이 표로 남는다.

사람이 토큰을 만들지 않는다. 실행 중에만 살아 있는 권한으로 올리므로 보관할
것도 교체할 것도 없다. 실행마다 판 이름이 달라지도록 회차가 붙는다 — 같은
이름을 다시 올리면 서버가 이미 받아 둔 이름의 내용이 조용히 바뀐다.

만드는 절차 자체는 `scripts/images-push.sh` 가 갖고 있고 실행이 그것을 부른다.
GitHub 이 멈췄거나 아직 올리지 않은 로컬 상태로 만들어야 할 때는 같은 스크립트를
직접 부른다. 그때는 `write:packages` 권한이 있는 토큰으로 먼저 로그인해야 한다.

```
IMAGE_REGISTRY=ghcr.io/we-meet-trip IMAGE_TAG=<판 이름> ./scripts/images-push.sh
```

**처음 한 번은 올린 뒤 패키지를 공개로 바꾼다** — 기본이 비공개라 그대로 두면
서버가 받으려 할 때 인증을 요구한다. 공개로 바꾸면 서버에는 레지스트리
자격증명을 두지 않아도 된다. 서버에 두는 비밀값이 하나라도 줄어드는 것이 이
선택의 이유다.

**이 전환은 되돌릴 수 없다.** 한 번 공개한 패키지는 다시 비공개로 만들 수
없다. 이미지 안에 소스가 들어 있으므로, 나중에 저장소를 비공개로 돌려도 코드는
이 경로로 계속 공개된다. 저장소를 닫을 가능성이 있다면 공개로 바꾸기 전에
정해야 한다 — 바꾼 뒤에는 선택지가 없다.

지금 공개해도 되는 근거는 둘이다. 저장소가 이미 공개라 소스가 새로 드러나지
않고, 이미지 안에 `.env`·서명키·인증키가 들어가지 않는다(빌드 무시 목록이
걸러 낸다).

`latest` 를 쓰지 않는다. 서버가 판 이름을 적지 않으면 받아오기가 그 자리에서
멈춘다. 그게 낫다 — 이름을 안 적은 채 뜨면 무엇이 도는지 어디에도 남지 않는다.


순서가 스크립트 안에 들어 있다. 특히 **hub 는 스스로 표를 만들지 않는데
표가 하나도 없어도 상태 확인에는 정상이라고 답한다.** 그 단계를 빠뜨리면
전부 초록으로 뜬 다음 요청을 받을 때 비로소 실패한다. 스크립트는 표가 실제로
생겼는지까지 보고 넘어간다.

### 5.4 확인

```
curl -s https://mapapp.duckdns.org/healthz                        # ok
curl -s -o /dev/null -w '%{http_code}' \
  https://mapapp.duckdns.org/actuator/health                      # 404 여야 한다
curl -s -o /dev/null -w '%{http_code}' \
  https://mapapp.duckdns.org/api/v1/schedules                     # 401 여야 한다
for i in $(seq 1 10); do curl -s -o /dev/null -w '%{http_code} ' \
  -X POST https://mapapp.duckdns.org/api/v1/trip/generate; done   # 뒤쪽에 429
sudo reboot                                                       # 90초 뒤 healthz 가 다시 ok
```

경로 엔진을 올렸으면 실제로 도로를 따르는지도 본다. 서버 안에서 직접 묻는다 —
바깥으로 열려 있지 않고, 열어서도 안 된다(인증이 없다).

```
curl -s 'http://127.0.0.1:5000/route/v1/foot/126.9780,37.5665;126.9850,37.5636?overview=full&geometries=geojson' \
  | grep -o '"code":"[^"]*"'                                      # "code":"Ok"
```

돌아온 경로의 점이 둘뿐이면 도로를 따르지 않은 것이다. 실제 도로를 따르면
짧은 구간에도 수십 개가 나온다.

`/actuator` 가 404 인지 보는 이유는, 그 경로가 열려 있으면 설정과 내부 상태가
그대로 나가기 때문이다. 토큰 없는 일정 조회가 401 인지 보는 이유는, 인증을
켰다고 믿는데 실제로는 열려 있는 상태를 잡기 위해서다 — 200 이 오면 켜지지
않은 것이다. 429 가 하나도 없으면 요청자 주소가 앞단 주소 하나로
접히고 있다는 뜻이다 — 3장의 헤더 처리를 다시 본다.

### 5.5 앱에 주소 알리기

**이 단계가 없으면 서버를 아무리 잘 세워도 앱은 오지 않는다.** 앱은 설치될 때
서버 주소를 품고 나가지 않고, 켤 때마다 게시된 설정 파일에서 읽는다. 그 파일이
아직 예전 임시 주소를 가리키고 있다.

```
# map-service-client/hosting/app_config.json 의 api_base_url 을 서버 주소로
firebase deploy --only hosting
```

**앱을 다시 만들 필요는 없다.** 다음에 앱을 켤 때 새 주소를 읽는다. 초대 링크가
앱을 열게 하는 검증 파일도 같은 자리에 있어, 서버 주소와 무관하게 그대로 돈다.

주의할 것이 하나 있다. 이 명령은 게시 폴더를 통째로 올린다. **웹을 다시
만들어서 올리면 안 된다** — 지도 페이지에 발급처 키가 글자 그대로 박혀 있어
그 순간 공개된다. 지금 게시된 것은 그 키가 없는 예전 판이라, 주소만 바꿔
올리는 것은 안전하다. 웹을 새로 올려야 할 일이 생기면 그 전에 키부터 뺀다.

### 5.6 백업

`scripts/pg-backup.sh` 를 새벽에 하루 한 번 돌린다. 이 스크립트는 같은 호스트에
쌓기만 하므로, 서버 자체가 사라지면 함께 사라진다. 오브젝트 스토리지로
옮기는 한 줄을 뒤에 붙인다. `.env` 도 함께, 다른 자리에 둔다 — 잃으면 예순
개가 넘는 값을 발급처마다 다시 받아야 한다.

경로 데이터는 백업하지 않는다. `scripts/osrm-rebuild.sh` 로 다시 만들 수 있다.

## 6. 정해 주셔야 하는 것

1. **duckdns 계정 토큰** — 이름 두 개(`mapapp`, `mapapptest`)는 정해졌다.
2. ~~인증서 알림 주소~~ — `mapadmin26@gmail.com` 으로 정했다.
3. **네이버 클라우드 서버 종류** — 크레딧으로 최소 운영이라면 4 vCPU / 8 GB 가
   `full` + 관리자까지의 하한이다. 경로 엔진이나 카메라를 올릴 계획이면 16 GB.
4. ~~이미지를 받아갈 곳~~ — `ghcr.io/we-meet-trip` 공개 패키지로 정했다.
   올리는 일은 레포의 배포 실행이 하므로 사람이 만들 토큰은 없다.
   첫 실행 뒤 패키지 셋을 공개로 바꾸는 일만 손으로 한다 — 되돌릴 수 없다.
5. **GCP 지역** — 무료 등급 서버는 미국 세 곳에서만 준다. 한국에서 쓰면
   왕복이 150ms 안팎 늘어난다. 기능 확인에는 지장이 없고 속도 감각만 다르다.
6. **`AUTH_ENFORCED`** — 켜면 앱의 모든 호출이 토큰을 실어 보내는지 먼저
   확인해야 한다. 빠진 곳이 있으면 그 화면만 401 이 된다.
7. **저장소를 컨테이너로 둘지, 클라우드 관리형으로 갈지.** 지금 구조는
   컨테이너를 전제한다 — 관리형으로 가면 초기화 스크립트를 손으로 돌려야 하고
   백업 스크립트도 접속 방식을 바꿔야 한다. 처음에는 컨테이너를 권한다.
