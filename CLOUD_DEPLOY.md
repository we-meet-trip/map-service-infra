# 클라우드 배포 — 운영은 네이버 클라우드, 시험은 GCP

늘 켜 두는 서버를 세우는 절차다. 맥에서 터널을 띄우던 방식은 주소가 열 때마다
달라지고 맥이 잠들면 함께 끊기므로 여기서는 쓰지 않는다.

두 환경은 **서로 다른 클라우드에 둔다.** 한쪽을 잘못 건드려도 다른 쪽이
멀쩡하고, 발급처 하루 한도를 나눠 쓰는 사고도 계정 자체가 달라 생기지 않는다.

## 1. 무엇을 어디에 두는가

| | 운영 | 시험 |
|---|---|---|
| 클라우드 | 네이버 클라우드 (VPC) | GCP |
| 구성 | `full` (저장소 2 + 앱 3 + 관문) + 앞단 | 같음. 경로 엔진·카메라·모니터링 제외 |
| 환경파일 | `.env` | `.env.test` (`make-test-env.sh` 로 서버에서 만든다) |
| 외부 호출 | 실제 발급처 | 전부 스텁 (`PLACES_STUB_MODE=true`) |

시험에서 외부를 끄는 이유는 비용이 아니라 **운영이 함께 멈추기 때문**이다.
기상청 키와 두루누비 키가 같은 발급 계정을 쓰므로, 시험이 하루 한도를
소진하면 운영의 예보 폴링과 코스 동기화가 같이 죽는다.

## 2. 서버 크기 — 실측에서 뽑은 값

같은 스택을 도커에 띄워 잰 유휴 사용량이다.

| 컨테이너 | 유휴 | 건 상한 |
|---|---|---|
| user (BFF) | 667 MiB | 2g |
| postgres | 217 MiB | 1g |
| hub | 114 MiB | 512m |
| agent | 104 MiB | 512m |
| redis | 10 MiB | 256m |
| 관문 | 9 MiB | 128m |
| **합계** | **약 1.1 GiB** | 약 4.4 GiB |

- **운영: 4 vCPU / 8 GB / 디스크 60 GB.** 상한 합이 4.4 GiB 이고 여기에 운영체제와
  도커, 그리고 이미지를 서버에서 직접 빌드하는 몫이 더 든다.
  경로 엔진이나 카메라를 함께 올릴 계획이면 16 GB 로 잡는다 — 경로 데이터만
  약 6.9 GB, 카메라 이미지가 약 3.2 GB 다.
- **시험: 2 vCPU / 4 GB / 디스크 50 GB.** 시험 오버레이가 상한을 절반 이하로
  낮춰 합이 약 2.4 GiB 다.
- **x86-64 를 쓴다.** 저장소 이미지와 경로 엔진 이미지가 amd64 전용이라
  ARM 서버에서는 흉내 내기로 돌아 느려진다.
- GCP 의 무료 등급 서버(e2-micro, 1 GB)로는 **뜨지 않는다.** 유휴만 1.1 GiB 다.

## 3. 바깥으로 여는 자리

애플리케이션 포트는 전부 루프백에만 묶여 있다. 그대로 둔다. 바깥으로 여는
것은 앞단의 80·443 둘뿐이고, 관리자 접속용 22 는 본인 주소에서만 연다.

| 방향 | 포트 | 대상 |
|---|---|---|
| 들어옴 | 22 | 본인 주소만 |
| 들어옴 | 80, 443 | 전체 (인증서 발급과 서비스) |
| 나감 | 443 | 발급처·모델·인증서 |
| 나감 | 80 | 서울 열린데이터광장 하나. 그쪽이 https 를 받지 않는다 |

**관문을 직접 열지 않는다.** 관문은 요청자 주소를 `CF-Connecting-IP` 헤더에서
읽고, 사설 대역에서 온 그 헤더를 믿는다. 관문이 바깥에 직접 노출되면 누구나
그 헤더를 적어 보내 분당 상한을 피할 수 있다. 실제로 확인했다 — 관문에 직접
보내면 적어 보낸 주소가 그대로 기록되고, 앞단을 거치면 지워진다.

앞단(`docker-compose.edge.yml`)은 그 헤더를 **먼저 지우고 자기가 본 실제
주소로 다시 채운다.** 그래서 관문 설정을 한 줄도 바꾸지 않고 상한이 계속
사용자별로 걸린다.

## 4. 주소 — 도메인이 없을 때

앱은 https 주소만 받는다(평문은 배포본에서 닫혀 있고 iOS 는 아예 열지 않는다).
도메인을 사지 않고 고정 https 주소를 얻는 길은 셋이다.

| 방법 | 주소 모양 | 대가 |
|---|---|---|
| 무료 동적 DNS (DuckDNS 등) + 앞단이 인증서 발급 | `이름.duckdns.org` | 무료. 제공자가 멈추면 함께 멈춘다 |
| IP 를 이름으로 바꿔 주는 서비스(sslip.io 등) | `1-2-3-4.sslip.io` | 무료. 서버 주소가 바뀌면 이름도 바뀐다 |
| Cloudflare 터널 + 도메인 | `api.내도메인` | 도메인 값만 든다. 들어오는 포트를 아예 안 열어도 된다 |

앞의 둘은 `EDGE_DOMAIN` 에 그 이름을 넣으면 앞단이 알아서 인증서를 받는다.
**어느 것을 쓸지는 정해 주셔야 한다.** 운영과 시험에 각각 하나씩 필요하다.

주소가 정해지면 앱을 다시 빌드할 필요가 없다 — 게시된 설정 파일의 주소만
바꾸면 앱이 다음 실행에서 새 주소를 쓴다.

## 5. 순서

### 5.1 서버 준비 (운영·시험 공통)

1. 우분투 22.04 서버를 만든다. 방화벽은 3장 표대로.
2. 도커 엔진과 compose 꾸러미를 **도커 공식 저장소에서** 넣는다.
   배포판이 주는 꾸러미는 이 저장소가 쓰는 오버레이 문법을 모른다.
3. 여섯 레포를 나란히 받는다(`map-service-*` 가 같은 부모 아래 있어야 한다).

### 5.2 비밀값 넣기

`.env` 를 **ssh 로 복사**한다(`scp`). 권한은 600.

- 클라우드 콘솔 화면에 붙여 넣지 않는다 — 브라우저 이력과 콘솔 기록에 남는다.
- ssh 로 `echo ... >> .env` 하지 않는다 — 셸 이력에 남는다.
- 시험 서버의 `.env.test` 는 복사하지 말고 **그 서버에서 만든다**:
  `./scripts/make-test-env.sh`. 본보기 파일에서만 파생시키므로 실제 키가
  넘어갈 길이 구조적으로 없다.

옮긴 뒤 지금 `.env` 에 있는 값들은 한 번 갈아 준다. 그 값들은 노트북과
임시 터널을 거쳤다.

운영 `.env` 에서 **반드시 바꿀 것**:

| 키 | 지금 | 운영 |
|---|---|---|
| `TESTER_SEED_ENABLED` | `true` | `false` |
| `AUTH_ENFORCED` | `false` | `true` |
| `ADMIN_SESSION_COOKIE_SECURE` | 없음 | `true` |
| `CORS_ALLOWED_ORIGINS` | 호스팅 주소 | 새 주소 포함 |
| `CHAT_INVITE_BASE_URL` | 없음(기본값 사용) | 새 초대 주소 |

`TESTER_SEED_ENABLED=true` 는 비밀번호가 공유된 계정 다섯을 부팅마다 만든다.
주소를 모르는 임시 터널 뒤에서는 가려져 있었지만 고정 주소에서는 그렇지 않다.

### 5.3 기동

```
./scripts/cloud-up.sh            # 운영
./scripts/cloud-up.sh --test     # 시험
./scripts/cloud-up.sh --edge     # 바깥 노출까지 함께
```

순서가 스크립트 안에 들어 있다. 특히 **hub 는 스스로 표를 만들지 않는데
표가 하나도 없어도 상태 확인에는 정상이라고 답한다.** 그 단계를 빠뜨리면
전부 초록으로 뜬 다음 요청을 받을 때 비로소 실패한다. 스크립트는 표가 실제로
생겼는지까지 보고 넘어간다.

### 5.4 확인

```
curl -s https://<주소>/healthz                       # ok
curl -s -o /dev/null -w '%{http_code}' https://<주소>/actuator/health   # 404 여야 한다
for i in $(seq 1 10); do curl -s -o /dev/null -w '%{http_code} ' \
  -X POST https://<주소>/api/v1/trip/generate; done                     # 뒤쪽에 429
sudo reboot                                          # 90초 뒤 healthz 가 다시 ok
```

`/actuator` 가 404 인지 보는 이유는, 그 경로가 열려 있으면 설정과 내부 상태가
그대로 나가기 때문이다. 429 가 하나도 없으면 요청자 주소가 앞단 주소 하나로
접히고 있다는 뜻이다 — 3장의 헤더 처리를 다시 본다.

### 5.5 백업

`scripts/pg-backup.sh` 를 새벽에 하루 한 번 돌린다. 이 스크립트는 같은 호스트에
쌓기만 하므로, 서버 자체가 사라지면 함께 사라진다. 오브젝트 스토리지로
옮기는 한 줄을 뒤에 붙인다. `.env` 도 함께, 다른 자리에 둔다 — 잃으면 예순
개가 넘는 값을 발급처마다 다시 받아야 한다.

경로 데이터는 백업하지 않는다. `scripts/osrm-rebuild.sh` 로 다시 만들 수 있다.

## 6. 정해 주셔야 하는 것

1. **주소** — 운영·시험 각각 하나. 4장의 셋 중 어느 방법인지.
2. **인증서 알림을 받을 메일 주소** (`EDGE_EMAIL`).
3. **네이버 클라우드 지역과 서버 종류** — 4 vCPU / 8 GB 로 잡을지, 경로 엔진과
   카메라까지 올려 16 GB 로 갈지.
4. **GCP 지역** — 서울(현실적인 응답 시간, 유료) 대 미국(무료 등급에 가깝지만
   왕복이 150ms 늘어난다).
5. **`AUTH_ENFORCED`** — 켜면 앱의 모든 호출이 토큰을 실어 보내는지 먼저
   확인해야 한다. 빠진 곳이 있으면 그 화면만 401 이 된다.
6. **저장소를 컨테이너로 둘지, 클라우드 관리형으로 갈지.** 지금 구조는
   컨테이너를 전제한다 — 관리형으로 가면 초기화 스크립트를 손으로 돌려야 하고
   백업 스크립트도 접속 방식을 바꿔야 한다. 처음에는 컨테이너를 권한다.
