# Local environment (실제 키 — 원격 push 절대 금지)
.env
.env.local
*.env.local
# 시험 스택 환경. scripts/make-test-env.sh 가 .env.example 에서 만든다.
# 발급처 키는 비워 두지만, 실수로 채운 값이 올라가지 않도록 함께 제외한다.
.env.test

# OSRM cached data (대용량, 빌드 결과)
osrm-data/
geofabrik/
*.pbf
*.osrm
*.osrm.*

# Logs
*.log

# 외부 노출 세션의 실행 상태(터널 pid·주소·로그). 실행마다 달라진다.
.serve/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.iml

# Python (방어적)
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 커밋 메시지 초안 (로컬 전용, 커밋 대상 아님)
commit_contents.md
