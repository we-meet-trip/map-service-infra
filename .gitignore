# Local environment (실제 키 — 원격 push 절대 금지)
.env
.env.local
*.env.local
# 시험 스택 환경파일. make-test-env.sh 가 .env.example 에서 다시 만들 수 있으므로
# 저장소에 담지 않는다. 담으면 비밀번호·토큰이 그대로 원격에 올라간다.
.env.test
# 손으로 뜬 사본도 같이 막는다. 이름만 다를 뿐 내용은 위와 같은데, 위 규칙은
# 정확히 일치할 때만 걸려서 사본은 그대로 추적 대상으로 올라온다.
.env.test.*
.env.bak*

# 학습 내보내기 산출물. 좌표가 담긴 실데이터라 저장소에 들어가면 안 된다.
# 파생물이므로 다시 만들 수 있다.

# agent 가 읽는 성향 통계. 파생물이며 실행 환경마다 다르다.

# OSRM cached data (대용량, 빌드 결과)
osrm-data/
geofabrik/
*.pbf
*.osrm
*.osrm.*

# Logs
*.log

# 외부 노출 세션의 실행 상태(터널 pid·주소·로그). 실행마다 달라진다.
.serve/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.iml

# Python (방어적)
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 커밋 메시지 초안 (로컬 전용, 커밋 대상 아님)
commit_contents.md

# 배포한 판을 한 줄씩 쌓는다. 되돌릴 때 이전 이름을 여기서 찾는다.
.deploy-history
